# Frontend - Promo Code Manager

React SPA приложение для управления промокодами с аналитикой.

## 🛠️ Технологический стек

- **React 19** — UI библиотека
- **TypeScript** — Типизированный JavaScript
- **Vite** — Сборщик и dev-сервер
- **Material-UI (MUI)** — UI компоненты
- **TanStack Query (React Query)** — Управление состоянием сервера
- **React Router** — Роутинг
- **Orval** — Кодогенерация API клиентов из Swagger
- **Material React Table** — Таблицы с серверной пагинацией
- **Axios** — HTTP клиент

## 📁 Архитектура (Feature-Sliced Design)

Проект использует архитектуру **Feature-Sliced Design (FSD)**:

```
src/
├── app/                    # Инициализация приложения
│   ├── App.tsx            # Корневой компонент
│   ├── providers/         # Провайдеры (Theme, Query)
│   └── router/            # Конфигурация роутинга
│
├── shared/                # Переиспользуемые модули
│   ├── api/               # API слой
│   │   ├── axios-instance.ts      # Axios instance
│   │   ├── constants/             # Константы (HTTP статусы, ошибки)
│   │   ├── interceptors/          # Request/Response interceptors
│   │   ├── types/                 # TypeScript типы для API
│   │   ├── utils/                 # Утилиты (refresh token)
│   │   └── generated/             # Сгенерированные API клиенты (Orval)
│   └── config/            # Конфигурация (query client)
│
├── entities/              # Бизнес-сущности (будут добавлены)
├── features/             # Функциональные возможности (будут добавлены)
├── widgets/              # Композитные блоки UI (будут добавлены)
└── pages/                # Страницы приложения (будут добавлены)
```

## 🏗️ Архитектурные решения

### 1. Feature-Sliced Design (FSD)

**Принципы:**
- **app/** — инициализация приложения (провайдеры, роутинг)
- **shared/** — переиспользуемые модули (API, утилиты, конфигурация)
- **entities/** — бизнес-сущности (User, PromoCode, Order)
- **features/** — функциональные возможности (авторизация, применение промокода)
- **widgets/** — композитные блоки UI (таблицы, формы)
- **pages/** — страницы приложения

**Преимущества:**
- ✅ Чёткое разделение ответственности
- ✅ Переиспользуемость компонентов
- ✅ Масштабируемость
- ✅ Понятная структура проекта

### 2. API Layer

**Структура:**
```
shared/api/
├── axios-instance.ts          # Основной axios instance
├── constants/                 # Константы
│   └── error-codes.const.ts  # HTTP статусы и сообщения об ошибках
├── interceptors/             # Axios interceptors
│   ├── request.interceptor.ts   # Добавление токена в запросы
│   └── response.interceptor.ts  # Обработка ответов и refresh token
├── types/                    # TypeScript типы
│   └── backend-response.types.ts  # Типы ответов с бэка
├── utils/                    # Утилиты
│   └── refresh-token.util.ts    # Функция для обновления токена
└── generated/                # Сгенерированные API клиенты (Orval)
```

**Особенности:**

#### Автоматический refresh token

При получении 401 (Unauthorized):
1. ✅ Проверяется, что это не запрос на `/auth/refresh` (избежание бесконечного цикла)
2. ✅ Проверяется флаг `_retry` (избежание повторных попыток)
3. ✅ Вызывается `refreshAccessToken()` для получения нового токена
4. ✅ Оригинальный запрос повторяется с новым токеном
5. ✅ При ошибке refresh token - очистка localStorage и редирект на `/login`

#### Типизация ответов

- `BackendSuccessResponse<T>` — успешный ответ `{ result: T }`
- `BackendErrorResponse` — ошибка `{ message: string, errors?: string[] }`
- Type guards для проверки типов ответов
- Проверка ошибок по константам (`HTTP_STATUS`, `ERROR_MESSAGES`)

#### Интеграция с Orval

- `customInstance` используется для кодогенерации API клиентов
- Автоматическая обработка `{ result: ... }` обёртки
- Поддержка отмены запросов через `cancel()`

### 3. TanStack Query (React Query)

**Конфигурация:**
- `refetchOnWindowFocus: false` — не обновлять при фокусе окна
- `retry: 1` — одна повторная попытка при ошибке
- `staleTime: 5 минут` — данные считаются свежими 5 минут

**Преимущества:**
- ✅ Кэширование запросов
- ✅ Автоматическая инвалидация кэша
- ✅ Оптимистичные обновления
- ✅ Интеграция с Orval для типобезопасных запросов

### 4. Material-UI (MUI)

**Использование:**
- Готовая система дизайна
- Адаптивность из коробки
- Material React Table для таблиц с серверной пагинацией

### 5. Кодогенерация API (Orval)

**Как работает:**
1. Backend предоставляет Swagger документацию (`/docs-json`)
2. Orval генерирует TypeScript типы и React Query hooks
3. Все запросы типобезопасны и соответствуют бэку

**Команды:**
```bash
# Генерация API клиентов из Swagger
pnpm run generate:api

# Генерация с watch режимом
pnpm run generate:api:watch
```

## 🚀 Быстрый старт

### Предварительные требования

- **Node.js** 18+
- **pnpm** (или npm/yarn)
- Запущенный backend на `http://localhost:3000`

### Установка и запуск

```bash
# Установка зависимостей
pnpm install

# Генерация API клиентов из Swagger (требуется запущенный backend)
pnpm run generate:api

# Запуск dev-сервера
pnpm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173` (или другой порт, если занят)

### Переменные окружения

Создайте файл `.env` в корне `frontend/`:

```env
VITE_API_BASE_URL=http://localhost:3000
```

## 📊 Основной функционал

### Планируемые возможности

- ✅ Авторизация и регистрация
- ✅ Управление промокодами (CRUD)
- ✅ Применение промокодов к заказам
- ✅ Аналитические таблицы:
  - Промокоды с метриками эффективности
  - Пользователи с агрегированной статистикой
  - История использований промокодов
- ✅ Серверная пагинация, сортировка, фильтрация
- ✅ Фильтрация по диапазону дат (пресеты: Сегодня, 7 дней, 30 дней, Произвольный)

## 🧪 Тестирование

```bash
# Линтинг
pnpm run lint

# Сборка для production
pnpm run build

# Превью production сборки
pnpm run preview
```

## 📝 Структура проекта

```
frontend/
├── src/
│   ├── app/                # Инициализация приложения
│   │   ├── App.tsx        # Корневой компонент
│   │   ├── providers/     # Провайдеры (Theme, Query)
│   │   └── router/        # Роутинг
│   │
│   ├── shared/            # Переиспользуемые модули
│   │   ├── api/          # API слой
│   │   └── config/       # Конфигурация
│   │
│   ├── entities/         # Бизнес-сущности (будут добавлены)
│   ├── features/         # Функциональные возможности (будут добавлены)
│   ├── widgets/          # Композитные блоки UI (будут добавлены)
│   └── pages/            # Страницы приложения (будут добавлены)
│
├── public/               # Статические файлы
├── orval.config.ts       # Конфигурация Orval
├── vite.config.ts        # Конфигурация Vite
└── package.json
```

## 🔧 Конфигурация

### Orval

Конфигурация в `orval.config.ts`:
- Источник: `http://localhost:3000/docs-json` (Swagger JSON)
- Выход: `./src/shared/api/generated`
- Клиент: `react-query`
- Mutator: `customInstance` из `axios-instance.ts`

### Vite

Конфигурация в `vite.config.ts`:
- React plugin для Fast Refresh
- TypeScript поддержка
- Path aliases (настроить при необходимости)

## 📖 Дополнительные ссылки

- [Backend README](../backend/README.md) — Документация по backend
- [Документация проекта](../documentation/README.md) — Общая документация
- [Задание](../documentation/task/TEST_TASK_PROMO_CODE_MANAGER_v2.1.md) — Описание задания

## 📝 Лицензия

Проект создан в рамках тестового задания.
